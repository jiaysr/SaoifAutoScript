# KekkaiUtilize 结界蹭卡 任务详解

## 1. 文件概述

**任务功能**: 自动执行结界蹭卡操作，包括：
- 进入寮结界
- 检查并替换满级式神
- 收取蹭卡经验
- 收取体力/经验盒子
- 收取寮资金和体力
- 智能选择最优结界卡

**入口文件**: `tasks/KekkaiUtilize/script_task.py`

---

## 2. 配置参数说明

**配置文件**: `tasks/KekkaiUtilize/config.py`

### UtilizeConfig 参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `utilize_rule` | UtilizeRule | DEFAULT | 蹭卡规则（斗鱼/太鼓优先） |
| `select_friend_list` | SelectFriendList | SAME_SERVER | 选择好友列表（同服/跨服） |
| `shikigami_class` | ShikigamiClass | N | 式神稀有度 |
| `shikigami_order` | int | 4 | 式神位置 |
| `harvest_guild_max_times` | int | 3 | 收取寮资源最大尝试次数 |
| `utilize_enable` | bool | True | 是否启用蹭卡 |
| `guild_ap_enable` | bool | True | 是否收取寮体力 |
| `guild_assets_enable` | bool | True | 是否收取寮资金 |
| `box_ap_enable` | bool | True | 是否收取体力盒子 |
| `box_exp_enable` | bool | True | 是否收取经验盒子 |
| `box_exp_waste` | bool | True | 经验溢出是否收取 |

### UtilizeRule 枚举

| 值 | 说明 |
|----|------|
| `DEFAULT` | 默认（斗鱼6>太鼓6>斗鱼5>太鼓5） |
| `TAIKO` | 太鼓优先 |
| `FISH` | 斗鱼优先 |

### SelectFriendList 枚举

| 值 | 说明 |
|----|------|
| `SAME_SERVER` | 同服好友 |
| `DIFFERENT_SERVER` | 跨服好友 |

---

## 3. 资源定义说明

**资源文件**: `tasks/KekkaiUtilize/assets.py`

### 图像资源 (RuleImage)

| 资源名 | 用途 | ROI位置 |
|--------|------|---------|
| `I_GUILD_INFO` | 寮信息 | (1157,599,97,91) |
| `I_GUILD_REALM` | 寮结界入口 | (1065,620,67,61) |
| `I_GUILD_AP` | 寮体力 | (483,314,22,22) |
| `I_GUILD_ASSETS` | 寮资金 | (315,311,23,28) |
| `I_SHI_GROWN` | 育成入口 | (595,291,35,38) |
| `I_BOX_AP` | 体力盒子 | (815,435,57,51) |
| `I_BOX_EXP` | 经验盒子 | (893,440,42,45) |
| `I_REALM_SHIN` | 结界皮肤 | (175,460,54,58) |
| `I_UTILIZE_EXP` | 蹭卡经验 | (583,144,58,43) |
| `I_UTILIZE_ADD` | 寄养添加 | (1149,54,74,67) |
| `I_U_FISH_3~6` | 斗鱼3-6星 | 左侧列表 |
| `I_U_TAIKO_3~6` | 太鼓3-6星 | 左侧列表 |
| `I_U_ENTER_REALM` | 进入结界 | (792,531,178,67) |
| `I_U_ADD_1/2` | 空位标识 | 右上角 |

### OCR资源 (RuleOcr)

| 资源名 | 用途 | ROI位置 |
|--------|------|---------|
| `O_UTILIZE_RES_TIME` | 剩余时间 | (1140,117,100,30) |
| `O_BOX_EXP` | 经验数量 | (654,538,179,39) |
| `O_CARD_NUM` | 卡片数值 | (800,421,150,33) |

---

## 4. 核心代码逐行解释

### ScriptTask.run() - 主入口

```python
def run(self):
    con = self.config.kekkai_utilize.utilize_config
    self.ui_get_current_page()
    self.ui_goto(page_guild)

    # 进入寮结界
    self.goto_realm()

    # 蹭卡操作
    if con.utilize_enable:
        self.check_utilize_add()

    # 检查满级式神
    self.check_max_lv(con.shikigami_class)

    # 收取蹭卡收获
    self.check_utilize_harvest()

    # 收取盒子
    self.check_box_ap_or_exp(con.box_ap_enable, con.box_exp_enable, con.box_exp_waste)

    # 收取寮资源
    self.recive_guild_ap_or_assets(con.harvest_guild_max_times)

    if not con.utilize_enable:
        self.set_next_run(task='KekkaiUtilize', finish=True, success=True)
    raise TaskEnd
```

### check_utilize_add() - 蹭卡主逻辑

```python
def check_utilize_add(self):
    con = self.config.kekkai_utilize.utilize_config
    while 1:
        self.utilize_add_count += 1
        if self.utilize_add_count >= 5:
            # 5次未找到合适卡片，5分钟后重试
            logger.warning('没有合适可以蹭的卡')
            self.set_next_run(task='KekkaiUtilize', target=datetime.now() + timedelta(minutes=5))
            return

        # 进入育成界面
        self.realm_goto_grown()
        self.screenshot()

        if not self.appear(self.I_UTILIZE_ADD):
            # 已有卡片，计算剩余时间
            remaining_time = self.O_UTILIZE_RES_TIME.ocr(self.device.image)
            next_time = datetime.now() + remaining_time
            self.set_next_run(task='KekkaiUtilize', target=next_time)
            return

        # 进入蹭卡界面
        if not self.grown_goto_utilize():
            logger.info('Utilize failed, exit')

        # 执行寄养
        if self.run_utilize(con.select_friend_list, con.shikigami_class, con.shikigami_order):
            self.back_guild()
            self.goto_realm()
        else:
            self.back_realm()
```

### run_utilize() - 执行寄养

```python
def run_utilize(self, friend, shikigami_class, shikigami_order):
    logger.hr('Start utilize')

    # 切换好友列表
    if self.first_utilize:
        self.swipe(self.S_U_END, interval=3)
        self.first_utilize = False
        # 先切换到另一个列表再切回来，刷新列表
        if friend == SelectFriendList.SAME_SERVER:
            self.switch_friend_list(SelectFriendList.DIFFERENT_SERVER)
            self.switch_friend_list(SelectFriendList.SAME_SERVER)
        else:
            self.switch_friend_list(SelectFriendList.SAME_SERVER)
            self.switch_friend_list(SelectFriendList.DIFFERENT_SERVER)
    else:
        self.switch_friend_list(friend)

    # 选择最优结界卡
    if not self._select_optimal_resource_card():
        return False

    # 重置计数
    self.utilize_add_count = 0

    # 进入好友结界
    if not self.appear(self.I_U_ENTER_REALM):
        return
    wait_timer = Timer(20)
    wait_timer.start()
    while 1:
        self.screenshot()
        if self.appear(self.I_U_ADD_1) or self.appear(self.I_U_ADD_2):
            break
        if wait_timer.reached():
            return

    # 判断空位
    stop_image = None
    if self.appear(self.I_U_ADD_1):
        stop_image = self.I_U_ADD_1
    elif self.appear(self.I_U_ADD_2):
        stop_image = self.I_U_ADD_2
    if not stop_image:
        return True  # 无空位

    # 切换式神类型并放置
    self.switch_shikigami_class(shikigami_class)
    self.set_shikigami(shikigami_order, stop_image)
    return True
```

### _select_optimal_resource_card() - 智能选卡

```python
def _select_optimal_resource_card(self):
    RESOURCE_PRESETS = {
        '斗鱼': [151, 143, 134, 126, 101, 84],
        '太鼓': [76,  76,  67,  67,  59,  50]
    }

    while True:
        self.screenshot()

        # 第一阶段：探索模式
        if self.ap_max_num == 0 and self.jade_max_num == 0:
            if self._current_select_best():
                return True  # 找到完美卡
            return False

        # 第二阶段：资源优先级判断
        ap_index = get_resource_index('斗鱼', self.ap_max_num, RESOURCE_PRESETS['斗鱼'])
        jade_index = get_resource_index('太鼓', self.jade_max_num, RESOURCE_PRESETS['太鼓'])

        # 决策优先级
        if ap_index <= jade_index:
            res_type, target = '斗鱼', self.ap_max_num
        else:
            res_type, target = '太鼓', self.jade_max_num

        # 第三阶段：确认选择
        if self._current_select_best(res_type, target, selected_card=True):
            return True
        else:
            return False
```

### check_max_lv() - 检查满级

```python
def check_max_lv(self, shikigami_class):
    self.realm_goto_grown()

    if self.appear(self.I_RS_LEVEL_MAX):
        # 存在满级式神，替换
        logger.info('Exist max level shikigami and replace it')
        self.unset_shikigami_max_lv()
        self.switch_shikigami_class(shikigami_class)
        self.set_shikigami(shikigami_order=7, stop_image=self.I_RS_NO_ADD)
    else:
        logger.info('No max level shikigami')

    # 检查是否有空位
    if self.detect_no_shikigami():
        logger.warning('There are no any shikigami grow room')
        self.switch_shikigami_class(shikigami_class)
        self.set_shikigami(shikigami_order=7, stop_image=self.I_RS_NO_ADD)

    # 回到结界界面
    while 1:
        self.screenshot()
        if self.appear(self.I_REALM_SHIN) and self.appear_multi_scale(self.I_SHI_GROWN):
            break
        if self.appear_then_click(self.I_UI_BACK_BLUE, interval=2.5):
            continue
```

---

## 5. 任务执行流程图

```
┌─────────────────────────────────────┐
│         KekkaiUtilize.run()         │
└─────────────────┬───────────────────┘
                  │
                  ▼
    ┌─────────────────────────────┐
    │  进入寮结界                 │
    └─────────────┬───────────────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
┌───────────────┐   ┌───────────────┐
│ utilize_enable│   │ check_max_lv  │
│   ↓ Yes       │   │ 检查满级式神  │
│check_utilize  │   └───────────────┘
│    _add       │           │
└───────────────┘           ▼
        │           ┌───────────────┐
        ▼           │check_utilize  │
┌───────────────┐   │   _harvest    │
│ 智能选卡      │   └───────────────┘
│ 进入好友结界  │           │
│ 放置式神      │           ▼
└───────────────┘   ┌───────────────┘
        │           │check_box_ap   │
        │           │   _or_exp     │
        │           └───────────────┘
        │                   │
        └─────────┬─────────┘
                  ▼
    ┌─────────────────────────────┐
    │  recive_guild_ap_or_assets  │
    │  raise TaskEnd              │
    └─────────────────────────────┘
```

---

## 6. 设计模式总结

### 1. 智能选卡算法
- 两阶段探索：先记录最优值，再确认选择
- 资源优先级决策：斗鱼vs太鼓
- 滑动遍历 + OCR识别

### 2. 状态机模式
- 检测满级式神
- 检测空位状态
- 检测卡片状态

### 3. 资源管理模式
- 收取体力/经验盒子
- 收取寮资金/体力
- 处理经验溢出

### 4. 定时任务模式
- 根据剩余时间设置下次执行
- 蹭卡失败时5分钟后重试

### 5. 组件组合模式
- 继承 `GameUi`, `ReplaceShikigami`
- 复用寮结界相关功能
