# TalismanPass 花合战 任务详解

## 1. 文件概述

**任务功能**: 自动收取花合战奖励，包括：
- 一键收取任务奖励
- 收取花合战等级奖励
- 收取1500签御魂奖励

**入口文件**: `tasks/TalismanPass/script_task.py`

---

## 2. 配置参数说明

**配置文件**: `tasks/TalismanPass/config.py`

### TalismanConfig 参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `level_reward` | LevelReward | TWO | 等级奖励选择 |
| `harvest_soul` | bool | False | 是否收取1500签御魂 |

### LevelReward 枚举

| 值 | 说明 |
|----|------|
| `ONE` | 蛇皮/青吉鬼 |
| `TWO` | 金币/勾玉 |
| `THREE` | 体力/樱饼 |

### Scheduler 参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `success_interval` | TimeDelta | 6小时 | 成功后间隔 |
| `failure_interval` | TimeDelta | 6小时 | 失败后间隔 |

---

## 3. 资源定义说明

**资源文件**: `tasks/TalismanPass/assets.py`

### 图像资源 (RuleImage)

| 资源名 | 用途 | ROI位置 |
|--------|------|---------|
| `I_TP_GET_ALL` | 领取全部按钮 | (903,599,70,71) |
| `I_RED_POINT_TASK` | 任务红点 | (1227,301,23,24) |
| `I_RED_POINT_DAY` | 今日红点 | (635,160,23,26) |
| `I_RED_POINT_WEEK` | 本周红点 | (803,161,24,25) |
| `I_RED_POINT_LEVEL` | 等级红点 | (1232,170,13,13) |
| `I_TP_LEVEL_1` | 一号奖励 | (203,435,122,59) |
| `I_TP_LEVEL_2` | 二号奖励 | (577,435,122,55) |
| `I_TP_LEVEL_3` | 三号奖励 | (967,433,109,61) |
| `I_TP_GOTO` | 前往按钮 | (995,254,85,34) |
| `I_TP_EXP` | 经验标识 | (922,254,32,36) |
| `I_OVERFLOW_CONFIRME` | 溢出确认 | (585,410,116,44) |
| `I_TP_SOUL_1` | 随机御魂 | (248,501,34,37) |
| `I_TP_SOUL_2` | 选择御魂 | (582,438,115,48) |
| `I_TP_SOUL_3` | 六星标识 | (313,489,188,33) |

---

## 4. 核心代码逐行解释

### ScriptTask.run() - 主入口

```python
def run(self):
    self.ui_goto_page(page_daily)  # 导航到每日任务页面
    con: TalismanConfig = self.config.talisman_pass.talisman

    # 收取全部奖励
    if self.in_task():
        self.get_all()

    # 收取花合战等级奖励
    self.get_flower(con.level_reward)

    # 收取1500签御魂
    if con.harvest_soul:
        self.ui_goto_page(page_main)
        self.harvest_soul()

    self.set_next_run(task='TalismanPass', success=True, finish=True)
    raise TaskEnd('TalismanPass')
```

### get_all() - 一键收取

```python
def get_all(self):
    self.screenshot()
    if not self.appear(self.I_TP_GET_ALL):
        logger.info('No appear get all button')
    self.ui_get_reward(self.I_TP_GET_ALL)
    logger.info('Get all reward')
    time.sleep(0.5)
```

### get_flower() - 收取等级奖励

```python
def get_flower(self, level: LevelReward = LevelReward.TWO):
    # 奖励映射
    match_level = {
        LevelReward.ONE: self.I_TP_LEVEL_1,
        LevelReward.TWO: self.I_TP_LEVEL_2,
        LevelReward.THREE: self.I_TP_LEVEL_3,
    }

    self.screenshot()
    if not self.appear(self.I_RED_POINT_LEVEL):
        logger.info('No any level reward')
        return

    logger.info('Appear level reward')
    self.ui_click(self.I_RED_POINT_LEVEL, self.I_TP_GET_ALL)

    check_timer = Timer(2)
    check_timer.start()
    while 1:
        self.screenshot()
        # 选择奖励类型
        if self.appear_then_click(match_level[level], interval=0.8):
            logger.info(f'Select {level} reward')
            # 处理溢出确认
            if self.appear_then_click(self.I_OVERFLOW_CONFIRME):
                pass
            check_timer.reset()
            continue

        # 领取奖励
        if self.ui_reward_appear_click(False):
            logger.info('Get reward')
            check_timer.reset()
            continue

        if check_timer.reached():
            logger.warning('No reward and break')
            break

        # 点击全部领取
        if self.appear_then_click(self.I_TP_GET_ALL, interval=2.1):
            logger.info('Get all reward')
            check_timer.reset()
            continue
```

### in_task() - 判断是否在任务界面

```python
def in_task(self) -> bool:
    self.screenshot()
    if self.appear(self.I_TP_GOTO) or self.appear(self.I_TP_EXP):
        return True
    return False
```

### harvest_soul() - 收取御魂

```python
def harvest_soul(self):
    logger.hr('Harvest soul')
    timer_harvest = Timer(5)

    while 1:
        self.screenshot()
        # 自选御魂
        if self.appear_multi_scale(self.I_TP_SOUL_1, scale_range=(0.8, 1.1)):
            logger.info('Select soul 2')
            # 点击随机御魂
            self.ui_click_multi_scale(self.I_TP_SOUL_1, stop=self.I_TP_SOUL_2, scale_range=(0.8, 1.1))
            # 选择中间御魂
            self.ui_click(self.I_TP_SOUL_2, stop=self.I_TP_SOUL_3, interval=3)
            # 确认选择
            self.ui_click_until_disappear(click=self.I_TP_SOUL_3)
            timer_harvest.reset()

        # 5秒无奖励则退出
        if not timer_harvest.started():
            timer_harvest.start()
        else:
            if timer_harvest.reached():
                logger.info('No more reward')
                return
```

---

## 5. 任务执行流程图

```
┌─────────────────────────────────────┐
│         TalismanPass.run()          │
└─────────────────┬───────────────────┘
                  │
                  ▼
    ┌─────────────────────────────┐
    │  导航到每日任务页面         │
    └─────────────┬───────────────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
┌───────────────┐   ┌───────────────┐
│ in_task()?    │   │ get_flower()  │
│   ↓ Yes       │   │ 等级奖励      │
│ get_all()     │   └───────────────┘
└───────────────┘           │
        │                   ▼
        │           ┌───────────────┐
        │           │ harvest_soul? │
        │           │   ↓ Yes       │
        │           │ 收取御魂      │
        │           └───────────────┘
        │                   │
        └─────────┬─────────┘
                  ▼
    ┌─────────────────────────────┐
    │  set_next_run()             │
    │  raise TaskEnd              │
    └─────────────────────────────┘
```

---

## 6. 设计模式总结

### 1. 策略模式
- `LevelReward` 枚举选择奖励类型
- 通过配置决定是否收取御魂

### 2. 超时控制
- `Timer(2)` 控制奖励收取间隔
- `Timer(5)` 控制御魂收取超时

### 3. 多尺度匹配
- `appear_multi_scale()` 支持不同分辨率
- `ui_click_multi_scale()` 自适应点击

### 4. 状态检测
- `I_RED_POINT_LEVEL` 检测红点
- `I_TP_GOTO` / `I_TP_EXP` 判断页面状态

### 5. 简单任务模式
- 单一职责：收取奖励
- 线性执行流程
- 快速完成
